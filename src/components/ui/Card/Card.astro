---
import "./Card.css";

interface Props {
  as?: "article" | "div" | "section";

  class?: string;
}

const {
  as: Tag = "article",

  class: className = "",
} = Astro.props;
---

<Tag class:list={["card", className]}>
  <slot />
</Tag>
